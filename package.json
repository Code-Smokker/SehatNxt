{
    "name": "sehat-next",
    "private": true,
    "scripts": {
        "dev": "pnpm -r dev",
        "build": "pnpm -r build"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}